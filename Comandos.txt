python -m venv parquaderoVenv
parqueaderoVenv\scripts\activate.bat
pip install dot-env flask
pip freeze > requirements.txt
pip install -r requirements.txt
flask run - servidor una vez
flask --app app run --debug - para activar el modo depuración, significa que esta reiniciando cada vez que detecte un cambio
